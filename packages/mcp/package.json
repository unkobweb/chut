{
  "name": "@chut/mcp",
  "version": "0.1.0",
  "description": "MCP server for chut: let an AI agent ask its human for a secret through a short-lived, browser-encrypted link.",
  "type": "module",
  "bin": { "chut-mcp": "dist/server.js" },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/server.ts",
    "test": "node test/protocol.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
